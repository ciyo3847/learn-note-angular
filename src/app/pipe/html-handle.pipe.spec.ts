import { HtmlHandlePipe } from './html-handle.pipe';

describe('HtmlHandlePipe', () => {
  it('create an instance', () => {
    const pipe = new HtmlHandlePipe();
    expect(pipe).toBeTruthy();
  });
});
